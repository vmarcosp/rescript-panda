(env
 (static
  (flags
   (:standard -ccopt -static))))

(executable
 (package rescript_panda_ppx)
 (name bin)
 (public_name rescript_panda_ppx)
 (modes
  (byte exe))
 (libraries rescript_panda_ppx ppxlib))
